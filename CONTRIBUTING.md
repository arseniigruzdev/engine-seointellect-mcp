# Contributing

1. Fork the repository and create a focused branch.
2. Run `npm ci`.
3. Make the smallest compatible change.
4. Add or update tests without using real Engine SEO Intellect account limits.
5. Run `npm run check` and `npm audit`.
6. Open a pull request describing API compatibility and security implications.

Do not commit tokens, customer URLs, private task results, or production logs.
